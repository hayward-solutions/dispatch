root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -o ./tmp/dispatch ./cmd/dispatch"
  bin = "./tmp/dispatch"
  full_bin = "./tmp/dispatch"
  include_ext = ["go", "html", "css", "js"]
  exclude_dir = ["tmp", "bin", "vendor"]
  delay = 1000

[misc]
  clean_on_exit = true
